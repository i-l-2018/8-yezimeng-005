git是一个软件，用途是版本管理。在电脑不联网的条件下，我们修改好的内容可以提交上去，成为一个新的版本，
同时，旧的版本也会被保留下来，便于日后查看。这样使得前后版本都被保留了下来，便于查看。
github是一个网站，许多程序员以及公司将自己的代码开源，更多的人可以对其关注，并参与到代码的编写过程中，
使得程序更加完善。
将两者绑定，可以将本地文件上传到github上，同时，github的使用者也可以将在网站上看到的文件下载到自己的电脑中：
另外，当文件的贡献者有多个的时候，不同步的问题，使用git也可以得到很好的解决。
而github作为一个线上工具，可以允许使用不同电脑登陆，这样，已被上传的文件可以被得到并利用。
